import "./LoadingFailed.scss";

const LoadingFailed = () => {
    return (
        <div className="error-message">
            Failed to load contents, please try again later.
        </div>
    )
}

export default LoadingFailed;